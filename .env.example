# BridgeAI Environment Configuration
# This file shows which environment variables are needed

# ============================================
# CEREBRAS API KEY (For Online Mode)
# ============================================
# Get your FREE API key at: https://cloud.cerebras.ai/
# 
# This enables the ONLINE-FIRST hybrid mode:
# - Online: Fast cloud AI (Cerebras llama-3.3-70b)
# - Automatic fallback to offline when internet drops
# 
# Without this key, BridgeAI runs in offline-only mode
# (still fully functional, but no automatic switching demo)
CEREBRAS_API_KEY=""

# ============================================
# Internal Configuration (Don't change these)
# ============================================
# Note: MODEL_PATH is the path INSIDE the Docker container
# The model file should be placed in the "models/" folder in your project root
# Docker automatically mounts it to /app/models/ inside the container
MCP_GATEWAY_URL=http://mcp-gateway:8080
MODEL_PATH=/app/models/llama-2-7b-chat.Q4_K_M.gguf
